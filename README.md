# Хрестики-Нолики 🎮 Online

Веб-гра «Хрестики-Нолики» з онлайн мультиплеєром на Python (Flask + Socket.IO).

## 🎮 Режими гри

| Режим | Опис |
|-------|------|
| 👥 **PvP (локальний)** | Два гравці на одному пристрої |
| 🤖 **PvE** | Гравець проти AI (алгоритм мінімакс — неможливо перемогти) |
| 🌐 **Online** | Мультиплеєр через WebSocket — створи кімнату, поділися кодом з другом! |

## 🛠 Технологічний стек

- **Backend:** Python 3.12 + Flask + Flask-SocketIO
- **Frontend:** Vanilla JS + CSS3 (темна тема, анімації)
- **WebSocket:** Socket.IO (eventlet async)
- **AI:** Алгоритм мінімакс
- **Deploy:** Render / Heroku / Railway

## 🚀 Локальний запуск

```bash
# 1. Клонуй репозиторій
git clone <url>
cd test-codmie

# 2. Створи віртуальне середовище
python -m venv venv
venv\Scripts\activate       # Windows
# source venv/bin/activate  # Linux/Mac

# 3. Встанови залежності
pip install -r requirements.txt

# 4. Запусти
python app.py
```

Відкрий **http://localhost:5000** у браузері.

## 🌍 Деплой на Render

1. Створи акаунт на [render.com](https://render.com)
2. Натисни **New → Web Service**
3. Підключи GitHub репозиторій
4. Налаштування:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT app:app`
   - **Environment:** додай змінну `SECRET_KEY` з будь-яким довгим рядком
5. Натисни **Deploy** — готово! 🎉

## 🌍 Деплой на Heroku

```bash
# Встанови Heroku CLI, потім:
heroku login
heroku create my-tic-tac-toe
git push heroku main
heroku open
```

## 🌍 Деплой на Railway

1. Зайди на [railway.app](https://railway.app)
2. **New Project → Deploy from GitHub Repo**
3. Railway автоматично знайде `Procfile` і задеплоїть
4. Додай змінну `SECRET_KEY` в Settings → Variables

## 📁 Структура проєкту

```
├── app.py              ← Сервер (Flask + SocketIO + AI)
├── wsgi.py             ← WSGI entry point для продакшену
├── requirements.txt    ← Python залежності
├── Procfile            ← Для Heroku / Render / Railway
├── runtime.txt         ← Версія Python
├── render.yaml         ← Конфіг для Render
├── templates/
│   └── index.html      ← HTML шаблон
├── static/
│   ├── style.css       ← Стилі (темна тема)
│   └── script.js       ← Клієнтська логіка + Socket.IO
└── README.md
```

## 🎯 Як грати онлайн

1. Відкрий сайт
2. Натисни **🌐 Створити кімнату**
3. Скопіюй 6-значний код кімнати
4. Відправ код другу
5. Друг натискає **🔗 Приєднатися до кімнати** і вводить код
6. Грайте! Після завершення — запросіть реванш 🔄

## 📝 Ліцензія

MIT — використовуй як хочеш!
